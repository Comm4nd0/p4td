# Base (dev + shared) requirements for p4td_backend.
# Production installs requirements-prod.txt, which pulls this file in via
# "-r requirements.txt" and adds prod-only extras (gunicorn, psycopg2, whitenoise).
# Pinned to the versions currently resolved in dev so dev/CI/prod stay aligned.

# Django core
Django==5.2.10
djangorestframework==3.16.1
djoser==2.2.3
django-cors-headers==4.3.1

# Environment variables
python-dotenv==1.2.2

# Image processing (thumbnails, EXIF rotation)
Pillow==10.3.0

# Push notifications
firebase-admin==7.3.0

# Rich text editor for admin
django-summernote==0.8.20.0

# reCAPTCHA v3 (contact form)
django-recaptcha==4.1.0

# HTML sanitisation (website user-generated content)
nh3==0.3.5

# AWS SNS signature verification for the DfT Street Manager roadworks webhook
# (api/sns.py). Already present transitively via firebase-admin, but pinned
# explicitly because security-critical code now depends on it directly.
#
# Must stay >= 46.0.5: earlier releases carry a statically linked vulnerable
# OpenSSL (PYSEC-2026-1284) and skip prime-order subgroup validation on public
# key load (PYSEC-2026-2141). Pinning the version that happened to be installed
# locally (42.0.7) put a known-vulnerable build under the signature check this
# dependency exists to perform, and the pip-audit job caught it.
cryptography==49.0.0

# Error monitoring (optional; only active when SENTRY_DSN is set — see settings.py)
# Pinned exactly: the old >=2.0,<3.0 range allowed 2.0.x, which predates the
# CVE-2024-40647 fix (env vars leaking to subprocesses, fixed in 2.8.0).
sentry-sdk==2.66.1
