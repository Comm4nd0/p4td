{$DOMAIN_NAME} {
    # Reverse proxy to Django
    reverse_proxy web:8000

    # Serve media files directly
    handle_path /media/* {
        root * /srv/media
        file_server
    }

    # Compression
    encode gzip
}
