# Production requirements for p4td_backend — the single install source.
# Install with: pip install -r requirements-prod.txt
#
# This is a strict superset of dev: it pulls in everything from requirements.txt
# (Django, DRF, Pillow, firebase-admin, nh3, sentry-sdk, …) and then adds the
# prod-only server/database/static extras below.

-r requirements.txt

# Production WSGI server
gunicorn==23.0.0

# PostgreSQL driver (prod DB; dev uses SQLite so this isn't in the base file)
psycopg2-binary>=2.9,<3.0

# Efficient static file serving
whitenoise==6.8.2
